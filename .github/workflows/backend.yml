name: Backend Spartan

on:
  # pull_request:
  #   branches: ["develop"]
  push:
    branches:
      - develop

jobs:
  build-back:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: "17"
          distribution: "adopt"

      - name: Build and test backend
        run: |
          cd /Users/theopierson/Sites/CDA/app-TP/spartan-back/src/main/java/com/onyx/spartan/controller/CustomersController.java

          ./mvn test
      # - name: Run Tests
      #   run: mvn test # Lance les tests de l'application
      #   working-directory: spartan-back # Se déplace dans le dossier du projet JAVA
